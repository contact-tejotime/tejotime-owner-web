# Backend API base (REST) and Socket.IO origin.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_SOCKET_URL=http://localhost:8080

# Origin to load Next static assets (CSS/JS/fonts) from.
# Leave empty for local dev; set to the deployed origin in production.
NEXT_PUBLIC_ASSET_PREFIX=
