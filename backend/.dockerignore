# Keep secrets and local build artifacts out of the image. Railway builds from a
# git checkout so these are already absent there, but a local `docker build`
# would otherwise bake .env / .env.render straight into a layer.
node_modules
dist
.env
.env.*
*.log

.git
.gitignore
tests
coverage
